# Local development (API running on host, DB in Docker)
DATABASE_URL=postgresql+psycopg://course:course@localhost:5432/course_service

# Docker Compose sets DATABASE_URL for the api service automatically.
